# Isolated environment for the reproducibility code.
# Does NOT use the system Python. Create a dedicated virtual environment:
#
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1      # PowerShell (Windows)
#   pip install -r requirements.txt
#   python generar_comparativa.py
#
# Recommended Python: 3.11 (conceptually tested on 3.10-3.13).
# Dependencies cover the main scripts and the archived notebook checkpoints.

numpy>=1.24,<3
scipy>=1.10
matplotlib>=3.7
pyDOE>=0.3.8      # used by archived notebook checkpoints; main scripts use internal LHS